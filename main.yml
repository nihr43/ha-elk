---
 - hosts: elasticsearch_nodes
   roles:
    - elasticsearch

 - hosts: kibana_nodes
   roles:
    - kibana

 - hosts: haproxy_a
   roles:
    - haproxy

 - hosts: haproxy_b
   roles:
    - haproxy
